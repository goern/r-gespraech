---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: callbacks.webhook.thoth-station.ninja
spec:
  group: webhook.thoth-station.ninja
  names:
    kind: Callback
    listKind: CallbackList
    plural: callbacks
    singular: callback
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Callback is the Schema for the callbacks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CallbackSpec defines the desired state of Callback
            properties:
              payload:
                type: string
              retryLimit:
                format: int32
                type: integer
              url:
                description: URL is the URL to be called back
                type: string
            required:
            - payload
            - url
            type: object
          status:
            description: CallbackStatus defines the observed state of Callback
            properties:
              phase:
                description: Current condition of the Shower.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
